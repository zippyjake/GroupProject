package com.example.groupproject;

public class Meep extends Car {
    public Meep(String type, String color, String shippingLocation, String paymentType, double totalCost) {
        super(type, color, shippingLocation, paymentType, totalCost);
        // Initialize Meep-specific instance variables
    }
}
