package com.example.groupproject;

public class Longitude extends Meep {
    // Longitude-specific instance variables

    public Longitude(String type, String color, String shippingLocation, String paymentType, double totalCost) {
        super(type, color, shippingLocation, paymentType, totalCost);
        // Initialize Longitude-specific instance variables
    }

    // Longitude-specific methods
}