package com.example.groupproject;

public class Manglers extends Meep {
    // Manglers-specific instance variables

    public Manglers(String type, String color, String shippingLocation, String paymentType, double totalCost) {
        super(type, color, shippingLocation, paymentType, totalCost);
        // Initialize Manglers-specific instance variables
    }

    // Manglers-specific methods
}